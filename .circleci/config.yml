version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
      - node/test
steps:
  - save_cache:
            keys:
              - myapp-{{ .Environment.CACHE_VERSION }}-{{checksum "yarn.lock" }}